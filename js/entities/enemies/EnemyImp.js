/**
 * ASTRA: Aether Guardians — Imp Enemy
 * Basic ground melee enemy
 */
import { EnemyBase } from '../EnemyBase.js';

export class EnemyImp extends EnemyBase {
  constructor(x, y, enemyData) {
    super(x, y, enemyData);
  }
}
